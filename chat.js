const $=id=>document.getElementById(id);let conversationId=localStorage.getItem("merqiva_chat_id")||crypto.randomUUID();localStorage.setItem("merqiva_chat_id",conversationId);function add(role,text){const d=document.createElement("div");d.className="msg "+(role==="user"?"user":"bot");d.textContent=text;$("messages").appendChild(d);$("messages").scrollTop=$("messages").scrollHeight}add("bot","Hi — I’m Merqiva’s customer assistant. Tell me what you sell, which market you target, or ask about our opportunity intelligence service.");$("chatForm").addEventListener("submit",async e=>{e.preventDefault();const message=$("message").value.trim();if(!message)return;const name=$("name").value.trim(),email=$("email").value.trim();add("user",message);$("message").value="";$("send").disabled=true;$("status").textContent="Thinking…";try{const r=await fetch("/api/chat",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({conversationId,message,name,email})});const d=await r.json();if(!r.ok||!d.success)throw Error(d.error||"Chat unavailable");add("bot",d.reply);if(d.leadCaptured)$("status").textContent="Your details were captured for the Merqiva team.";else $("status").textContent="";}catch(err){add("bot","I’m unable to complete that request right now. You can email sales@merqivaintel.com.");$("status").textContent=err.message;}finally{$("send").disabled=false;$("message").focus();}});