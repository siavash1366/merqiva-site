const session=localStorage.getItem('admin_session');if(!session)location.href='/admin/index.html';const $=id=>document.getElementById(id);const esc=v=>String(v??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');
async function req(url,opt={}){const h=new Headers(opt.headers||{});h.set('Authorization','Bearer '+session);const r=await fetch(url,{...opt,headers:h});if(r.status===401){localStorage.removeItem('admin_session');location.href='/admin/index.html';return;}const d=await r.json();if(!r.ok||d.success===false)throw Error(d.error||'Request failed');return d;}
async function load(){try{const d=await req('/api/admin/campaigns');const a=d.campaigns||d.items||[];$('list').innerHTML=a.length?a.map(c=>`<div class="list-row"><div><strong>${esc(c.name||c.id)}</strong><span class="pill">${esc(c.status||'Draft')}</span></div><p>${esc(c.subject||'No subject')} · ${esc(c.id)}</p><div class="toolbar"><a class="button-link" href="/admin/campaign-audience.html?campaign=${encodeURIComponent(c.id)}">Audience</a><button data-action="prepare" data-id="${esc(c.id)}">Prepare</button><button data-action="run" data-id="${esc(c.id)}">Run batch</button></div></div>`).join(''):'<p class="muted">No campaigns yet.</p>';$('list').querySelectorAll('button[data-action]').forEach(b=>b.onclick=async()=>{try{const id=b.dataset.id;if(b.dataset.action==='prepare'){await req('/api/admin/prepare-campaign',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({campaignId:id})});}else{if(!confirm('Run this campaign batch now?'))return;await req('/api/admin/run-campaign-batch',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({campaignId:id,confirm:'RUN_BATCH',limit:5})});}await load();}catch(e){$('status').textContent=e.message;}});$('status').textContent='Updated '+new Date().toLocaleString();}catch(e){$('status').textContent=e.message;}}
$('create').onclick=async()=>{try{const body={name:$('name').value.trim(),subject:$('subject').value.trim(),body:$('body').value};if(!body.name||!body.subject||!body.body)throw Error('Name, subject and content are required');await req('/api/admin/campaigns',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});$('name').value='';$('subject').value='';$('body').value='';await load();}catch(e){$('status').textContent=e.message;}};$('refresh').onclick=load;load();